import java.util.Scanner;
public class StepTracker {
    Scanner scanner = new Scanner(System.in);
    int targetSteps = 10_000;
   // int[] days = new int[30]; // обьявил масив с именем -days- и 30 целыми числами
    /* MonthData[] monthToData;
    public StepTracker() {  // Имя "MonthData" масив с месяцем
        monthToData = new MonthData[12];
        for (int i = 0; i < monthToData.length; i++) {
            monthToData[i] = new MonthData();
        }
    }

       class MonthData {
         daysMontch[] daysToMontch;
         public DaysDate() { // Имя "daysToMontch" масив для дней
             daysToMontch = daysMontch[30];
             for (int i = 0; i < daysToMontch.length; i++) {
                 daysToMontch[i] = new daysMontch(); */
             }






      //    System.out.println("Введите количество шагов: ");
      //    monthToData[month].dayData[day]=scanner.nextInt();